package BIF.SWE1.enums;

/**
 * Enum of all valid HTTP methods
 */

public enum MethodType {
    POST,
    GET,
    HEAD,
    PUT,
    DELETE,
    CONNECT,
    OPTIONS,
    TRACE,
    PATCH,
    INVALID
}